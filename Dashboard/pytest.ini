[pytest]
# Make `import app` / `import config` resolve from the Dashboard/ folder.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q
# The 'testing' config (config.py) forces an in-memory SQLite DB, so tests
# never touch the real MySQL data regardless of .env settings.
