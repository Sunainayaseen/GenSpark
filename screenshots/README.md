# Screenshots

Add app screenshots here for submission (landing page, build configurator,
checkout, admin/vendor dashboards, chatbot). Automated capture wasn't possible
in this environment (no headless browser available) — grab these manually
from the running app at http://localhost:5173.
